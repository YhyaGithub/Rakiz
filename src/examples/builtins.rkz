# Console
print("Hello World!") # output: Hello World!

# Date
time() # returns the time as number.

# Math
min(1, 2) # returns the minimum value, required two numbers only. output: 1
max(1, 2) # returns the maximum value, required two numbers only. output: 2

# Validations
isNull(null) # output: true
